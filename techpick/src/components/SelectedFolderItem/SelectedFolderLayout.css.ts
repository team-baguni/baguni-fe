import { style } from '@vanilla-extract/css';

export const selectedFolderLayoutStyle = style({
  display: 'inline-flex',
  alignItems: 'center',
  maxWidth: '264px',
  height: '20px',
  borderRadius: '4px',
});
